<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>관리자 페이지</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icon-->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    <!-- Pretendard 폰트 추가 -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard/dist/web/static/pretendard.css">
    <!-- 📌 Quill.js 스타일 & 스크립트 추가 -->
    <link href="https://cdn.quilljs.com/1.3.6/quill.snow.css" rel="stylesheet">
    <!-- Custom CSS -->
    <link href="/css/style.css" rel="stylesheet">
</head>
<body>
    <!-- 상단 네비게이션 바 -->
    <nav class="navbar navbar-light bg-light px-3">
        <a class="navbar-brand d-lg-none" href="/">
            <img src="/images/logo.svg" alt="브레인센스_로고">
        </a>
        <a class="navbar-brand d-none d-lg-block" href="/main">
            <img src="/images/logo.svg" alt="브레인센스_로고">
        </a>
        <div class="d-flex justify-content-end align-items-center gap-2">
            <button class="btn btn-primary d-none d-lg-block" onclick="location.href='https://www.naver.com'">
                회사 홈페이지로 이동
            </button>
            <button class="btn btn-primary d-lg-none" onclick="location.href='https://www.naver.com'">
                <i class="bi bi-house text-white"></i>
            </button>
            <button class="btn btn-danger d-none d-lg-block" onclick="location.href='/admin/logout'">
                로그아웃
            </button>
            <button class="btn btn-danger d-lg-none" onclick="location.href='/admin/logout'">
                <i class="bi bi-box-arrow-right text-white"></i>
            </button>
        </div>
    </nav>
    <div class="container mt-5">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2>게시글 관리</h2>
            <button class="btn btn-success" data-bs-toggle="modal" data-bs-target="#newPostModal">새로운 게시글 작성하기</button>
        </div>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>순번</th>
                    <th>제목</th>
                    <th>관리</th>
                    <th>생성일자</th>
                    <th>수정일자</th>
                </tr>
            </thead>
            <tbody>
                <% posts.forEach((post, index) => { %>
                <tr>
                    <td><%= index + 1 %></td>
                    <td>
                        <a href="#" 
                           class="text-decoration-none post-link" 
                           data-bs-toggle="modal" 
                           data-bs-target="#viewPostModal"
                           data-title="<%= post.title %>"
                           data-source="<%= post.source %>"
                           data-content='<%= post.content %>'
                           data-attachments='<%- JSON.stringify(post.attachments || []) %>'
                           data-created-at="<%= post.createdAt %>"
                           data-updated-at="<%= post.updatedAt %>">
                            <%= post.title %>
                        </a>
                    </td>
                    
                    
                    <td>
                        <button class="btn btn-primary btn-sm" data-bs-toggle="modal" data-bs-target="#editModal"
                            data-id="<%= post._id %>"
                            data-title="<%= post.title %>"
                            data-source="<%= post.source %>"
                            data-content="<%= post.content %>"
                            data-attachments='<%= JSON.stringify(post.attachments || []) %>'
                            data-created-at="<%= post.createdAt %>"
                            data-updated-at="<%= post.updatedAt %>">
                            수정하기
                        </button>
                        <button class="btn btn-danger btn-sm" onclick="confirmDelete('<%= post._id %>')">삭제하기</button>
                    </td>
                    <!-- 📌 생성일자 표시 -->
                    <td><%= new Date(post.createdAt).toLocaleString("ko-KR", { year: "numeric", month: "2-digit", day: "2-digit", hour: "2-digit", minute: "2-digit", second: "2-digit" }) %></td>
                    <!-- 📌 수정일자 표시 (생성일자와 다를 경우만 표시) -->
                    <td>
                        <% if (post.updatedAt && post.createdAt !== post.updatedAt) { %>
                            <%= new Date(post.updatedAt).toLocaleString("ko-KR", { year: "numeric", month: "2-digit", day: "2-digit", hour: "2-digit", minute: "2-digit", second: "2-digit" }) %>
                        <% } else { %>
                            -
                        <% } %>
                    </td>
                </tr>
                <% }) %>
            </tbody>
            
        </table>
    </div>
    
    <!-- 게시글 보기 모달 -->
    <div class="modal fade" id="viewPostModal" tabindex="-1" aria-labelledby="viewPostModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-header d-flex justify-content-between align-items-center w-100">
                    <h5 id="viewPostTitle" class="mb-0"></h5> <!-- 제목 왼쪽 정렬 -->
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="d-flex justify-content-end align-items-center me-4">
                    <small class="text-muted text-end">
                        <span id="viewPostTime"></span>
                        <span class="mx-2">|</span>
                        <span id="viewPostSource"></span>
                    </small>
                </div>
                
                
                <div class="modal-body">
                    <p id="viewPostContent" class="content-text">여기에 내용이 표시됩니다.</p>
                    <!-- 📌 첨부 파일 목록 (파일 다운로드 링크) -->
                    <div id="viewPostAttachments" class="mt-3"></div>
                </div>
            </div>
        </div>
    </div>
    
    <!-- 게시글 작성 모달 -->
    <div class="modal fade" id="newPostModal" tabindex="-1" aria-labelledby="newPostModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="newPostModalLabel">새로운 게시글 작성</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <form id="newPostForm" enctype="multipart/form-data">
                        <div class="mb-3">
                            <label for="newPostTitle" class="form-label">제목</label>
                            <input type="text" class="form-control" id="newPostTitle" name="title" required>
                        </div>
                        <div class="mb-3">
                            <label for="newPostSource" class="form-label">출처</label>
                            <input type="text" class="form-control" id="newPostSource" name="source"  placeholder="출처를 입력하세요" required>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">내용</label>
                            <!-- Quill 에디터 추가 -->
                            <div id="editor"></div>
                            <!-- 실제 데이터를 전송할 숨겨진 필드 -->
                            <input type="hidden" id="newPostContent" name="content">
                        </div>
                        <div class="mb-3">
                            <label for="newPostAttachments" class="form-label">파일 첨부</label>
                            <!-- input의 파일 크기 제한 설정 -->
                            <input type="file" class="form-control" id="newPostAttachments" name="attachments" multiple accept=".png, .jpg, .jpeg, .gif, .webp, .svg, .pdf, .docx" onChange="validateFileSize(this)">
                            <small class="text-muted">📌 업로드 가능 확장자: <strong>.png, .jpg, .jpeg, .gif, .webp, .svg</strong></small><br>
                            <small class="text-muted">📁 업로드 가능 크기: <strong>5MB 이내 파일 크기만 업로드 가능</strong></small>
                        </div>
                        <div class="mb-3">
                            <div id="newAttachmentPreview"></div> <!-- 새 이미지 미리보기 -->
                        </div>
                        <button type="submit" class="btn btn-success">저장</button>
                    </form>
                </div>
            </div>
        </div>
    </div>

   <!-- 게시글 수정 모달 -->
    <div class="modal fade" id="editModal" tabindex="-1" aria-labelledby="editModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="editModalLabel">게시글 수정</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <form id="editForm" enctype="multipart/form-data">
                        <input type="hidden" id="postId" name="postId">

                        <div class="mb-3">
                            <label for="postTitle" class="form-label">제목</label>
                            <input type="text" class="form-control" id="postTitle" name="title" required>
                        </div>
                        <div class="mb-3">
                            <label for="postSource" class="form-label">출처</label>
                            <input type="text" class="form-control" id="postSource" name="source" required>
                        </div>

                        <div class="mb-3">
                            <label class="form-label">내용</label>
                            <!-- Quill 에디터 추가 -->
                            <div id="editEditor"></div>
                            <!-- 숨겨진 필드 -->
                            <input type="hidden" id="editPostContent" name="content">
                        </div>

                        <div class="mb-3">
                            <label class="form-label">기존 첨부 파일</label>
                            <div id="editAttachmentsContainer"></div> <!-- 기존 파일 표시 -->
                        </div>
                        
                        <div class="mb-3">
                            <label for="editPostAttachments" class="form-label">새 파일 추가</label>
                            <input type="file" class="form-control" id="editPostAttachments" name="attachments" multiple accept=".png, .jpg, .jpeg, .gif, .webp, .svg, .pdf, .docx, .txt">
                        </div>

                        <div class="mb-3">
                            <div id="attachmentPreview"></div> <!-- 새 이미지 미리보기 -->
                        </div>
                        

                        <button type="submit" class="btn btn-primary">수정 완료</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
    
    <footer class="bg-light text-center text-lg-start mt-5">
        <div class="text-center p-3" style="background-color: rgba(0, 0, 0, 0.05);">
            © 2025 브레인센스. All rights reserved.
        </div>
    </footer>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.quilljs.com/1.3.6/quill.min.js"></script>
    <script src="/js/admin_crud_script.js"></script>
    <script src="/js/admin_crud_file.js"></script>
    
</body>
</html>

<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>관리자 페이지 - 주문 목록</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icon -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    <!-- Pretendard 폰트 추가 -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard/dist/web/static/pretendard.css">
    <!-- Custom CSS -->
    <link href="/css/style.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-light bg-light px-3">
        <a class="navbar-brand d-lg-none" href="/">
            <img src="/images/logo.svg" alt="브레인센스_로고">
        </a>
        <a class="navbar-brand d-none d-lg-block" href="/main">
            <img src="/images/logo.svg" alt="브레인센스_로고">
        </a>
        <div class="d-flex justify-content-end align-items-center gap-2">
            <button class="btn btn-danger d-none d-lg-block" onclick="location.href='/logout'">
                로그아웃
            </button>
        </div>
    </nav>
    
    <div class="container mt-5">
        <h2>주문 목록</h2>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>순번</th>
                    <th>회사명</th>
                    <th>유형</th>
                    <th>상태</th>
                    <th>관리</th>
                </tr>
            </thead>
            <tbody>
                <% orders.forEach((order, index) => { %>
                <tr>
                    <td><%= index + 1 %></td>
                    <td><%= order.companyName %></td>
                    <td><%= order.orderType %></td>
                    <td>
                        <select class="form-select" id="status-<%= order._id %>" onchange="updateStatus('<%= order._id %>')">
                            <option value="처리전" <%= order.status === '처리전' ? 'selected' : '' %>>처리전</option>
                            <option value="완료" <%= order.status === '완료' ? 'selected' : '' %>>완료</option>
                        </select>
                    </td>
                    <td>
                        <button class="btn btn-info btn-sm" data-bs-toggle="modal" data-bs-target="#orderDetailsModal"
                            data-id="<%= order._id %>"
                            data-company-name="<%= order.companyName %>"
                            data-order-type="<%= order.orderType %>"
                            data-details="<%= order.details %>"
                            data-status="<%= order.status %>"
                            data-email="<%= order.companyEmail %>"
                            data-phone="<%= order.companyPhone %>">
                            보기
                        </button>
                    </td>
                </tr>
                <% }) %>
            </tbody>
        </table>
    </div>

    <!-- 주문 상세 보기 모달 -->
    <div class="modal fade" id="orderDetailsModal" tabindex="-1" aria-labelledby="orderDetailsModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 id="orderDetailsModalLabel">주문 상세보기</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <p><strong>회사명:</strong> <span id="modalCompanyName"></span></p>
                    <p><strong>유형:</strong> <span id="modalOrderType"></span></p>
                    <p><strong>세부 사항:</strong> <span id="modalDetails"></span></p>
                    <p><strong>회사 이메일:</strong> <span id="modalCompanyEmail"></span></p>
                    <p><strong>회사 전화번호:</strong> <span id="modalCompanyPhone"></span></p>
                    <p><strong>상태:</strong> <span id="modalStatus"></span></p>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="/js/admin_order_list.js"></script>
</body>
</html>
